# Device Gateway

DeviceGatewayAdapter implementations (Matter/MQTT/local HTTP/Bluetooth/vendor APIs). See docs/architecture/10-IOT.md. No adapter in Phase 1 — the deny-by-default device trust model has nothing to connect to yet.
