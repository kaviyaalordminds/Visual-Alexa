{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Phase 10 P0-1 (docs/phase-10/ARCHITECTURE-AUDIT.md §1): the only native privilege the VEYRA desktop shell has is spawning its own bundled Local API sidecar binary — nothing else. No filesystem, network, or other shell-execute permission is granted.",
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "binaries/veyra-local-api",
          "sidecar": true,
          "args": false
        }
      ]
    }
  ]
}
