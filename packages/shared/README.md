# Shared

Reserved for genuinely cross-cutting utilities that don't belong in
`packages/contracts` (which is data contracts only, no behavior) and
aren't specific to any one service. Empty in Phase 1 — CLAUDE.md's
dependency rules mean this package should only gain content when at least
two independent consumers actually need the same utility, not
speculatively.
